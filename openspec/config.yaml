schema: spec-driven

context: |
  Tech stack: undetected (greenfield project, no code yet)
  Architecture: undetected
  Testing: none configured
  Style: none established
  Workspace: C:\Users\messi\OneDrive\Documentos\Proyectos\ZeroDashboard (not a git repo)

strict_tdd: false

testing:
  projects: []
  note: "Zero projects discovered — the workspace root is empty. Re-run /gentle-sdd-init after scaffolding real code to populate this and re-evaluate strict_tdd."

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
